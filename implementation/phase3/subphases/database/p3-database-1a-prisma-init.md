# p3-database-1a-prisma-init

> **Phase:** 3 — Backend & Persistence
> **Group:** Database
> **Status:** ⬜ Not Started
> **Dependencies:** Phase 2 complete

## Objective

Install Prisma, initialize with PostgreSQL, configure environment variables.

## Scope

- Install `prisma` and `@prisma/client`
- Run `npx prisma init`
- Configure `DATABASE_URL` in `.env` / `.env.example`
- Update `.gitignore` for `.env`

## Preliminary Files

| Action | Path |
|--------|------|
| Create | `prisma/schema.prisma` |
| Create | `.env.example` |
| Modify | `package.json` |
| Modify | `.gitignore` |

## Detailed task breakdown to be completed at phase start.
